# Repository map

- `CLAUDE.md` owns conventions and platform-owned files.
- `docs/design.md` owns the landing reel visual registers.
- Check with `node --test src/lib/*.test.mjs && npm run build`.

Use `scratch/` for temporary thinking artifacts.
Do not treat `scratch/` as product source.
